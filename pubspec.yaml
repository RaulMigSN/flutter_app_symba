name: flutter_app_symba
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_bloc: ^9.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  collection: ^1.19.1

dev_dependencies:
  build_runner: ^2.4.6
  hive_generator: ^2.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true